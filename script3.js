const meuObjeto = {
    primeiraPropiedade: "sou a primeira propiedade.",
    segundaPropiedade: "mentira, eu que sou."
};

//terceiro video logo em baixo, viu?

//const meuObjet2 = []
//const meuObjeto3 = {}

//meuObjeto3.fruta = "tomate"
//meuObjeto3[0] = "jabuticaba"

//ultima parte!!!!

function somaDoisNumeros(numero1, numero2){
    return numero1 + numero2;
}
console.log(somaDoisNumeros(5, 10))

somaDoisNumeros(5, 10)

//atividade

function decodificarAtividade(variaveComTexto){ 
    let primeiraDecodificaao = variavelComTexo.split(',').toString()
    let segundaDecodificacao = primeiraDecodificacao.split(',')
    let terceiraDecodificacao = segundaDecodificacao.filter(variaveComTexto); {
        if (primeiraDecodificaao = ","){
            return true
        }
        if(segundaDecodificacao = ","){
            return true}
        
        return Number.isNaN(Number(terceiraDecodificacao))
    }
}
let primeiraDecodificacao = segundaDecodificacao.join('4')

console.log(variaveComTexto)


decodificarAtividade(atividadeCodificada)

